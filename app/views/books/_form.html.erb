<%= form_with(model: book) do |form| %>
  <% if book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-2">
    <%= form.label :name, class:"form-label" %>
    <%= form.text_field :name, class:"form-control border-dark-subtle bg-body-tertiary" %>
  </div>

  <div class="my-2">
    <%= form.label :author, class:"form-label" %>
    <%= form.text_field :author, class:"form-control border-dark-subtle bg-body-tertiary" %>
  </div>

  <div class="my-2">
    <%= form.number_field :user_id, id: :book_user_id, type: :hidden, value:current_user.id, class:"form-control border-dark-subtle bg-body-tertiary" %>
  </div>


  <div>
    <%= form.submit class:"btn btn-primary my-2"%>
  </div>
<% end %>
